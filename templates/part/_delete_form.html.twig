<form method="post" action="{{ path('part_delete', {'id': part.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ part.id) }}">
    <button class="btn">Delete</button>
</form>
