{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Modifier {{ vehicle.name }}{% endblock %}

{% block body %}
    <a href="{{ path('vehicle_parts', {vehicle: vehicle.id}) }}">Retour à la liste</a>
	<div class="text-align-center"><h1 class="title">Modifier {{ vehicle.name }}</h1></div>
	{{ include('vehicle/_form.html.twig', {'button_label': 'Modifier', 'showDeleteForm': true}) }}
{% endblock %}
