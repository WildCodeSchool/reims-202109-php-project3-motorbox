{% extends 'base.html.twig' %}

{% block title %}Modifier
	{{ vehicle.name }}
{% endblock %}

{% block body %}

    <a href="{{ path('vehicle_parts', {vehicle: vehicle.id}) }}">Retourner à la liste</a>

    <div class="text-align-center">
        <h1 class="title-form">Modifier {{ vehicle.name }}</h1>
    </div>
    {{ include('vehicle/_form.html.twig', {'button_label': 'Modifier'}) }}
    {% include "_navbar.html.twig" %}
{% endblock %}
