<form method="post" action="{{ path('vehicle_parts_delete', {vehicle: vehicle.id, part: part.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ part.id) }}">
    <button class="button">Delete</button>
</form>
