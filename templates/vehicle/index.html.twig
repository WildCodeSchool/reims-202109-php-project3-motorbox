{% extends 'base.html.twig' %}

{% block title %}Vehicle index{% endblock %}

{% block body %}
    <section class="home">
        {% for vehicle in vehicles %}
            <a href="{{ path('part_index') }}" ><p class="box">{{ vehicle.name }}<br>{{ vehicle.brand }} {{ vehicle.model }}</p></a>
        {% endfor %}
        <a class="aBox" href="{{ path('vehicle_new') }}"> Ajouter </a>
    </section>

    {% include "_navbar.html.twig" %}
{% endblock %}
