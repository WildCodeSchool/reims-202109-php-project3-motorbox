{% extends 'base.html.twig' %}

{% block title %}Vehicle index{% endblock %}

{% block body %}
    <section class="home">
        {% for vehicle in user.vehicles %}
            <a class="conteneur-link" href="{{ path('part_index') }}" ><p class="box">{{ vehicle.name }}<br>{{ vehicle.brand }} {{ vehicle.model }}</p></a>
        {% endfor %}
            <a class="aBox conteneur-link" href="{{ path('vehicle_new') }}"> Ajouter </a>
    </section>

    {% include "_navbar.html.twig" %}
{% endblock %}
