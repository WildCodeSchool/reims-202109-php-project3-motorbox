{% extends 'base.html.twig' %}

{% block title %}Modifier {{ part.name }}{% endblock %}

{% block body %}

    <a href="{{ path('vehicle_parts', {vehicle: vehicle.id}) }}">Retour à la liste</a>
    <div class="text-align-center">
        <h1 class="title-form">Modifier {{ part.name }}</h1>

        {{ include('part/_form.html.twig') }}

        <div class="button-bar">
            {{ include('vehicle/_delete_parts_form.html.twig') }}
            <button type="submit" form="part_form" class="button">Modifier</button>
        </div>
    </div>
    <div class="navbar-container">
        {% include "_navbar.html.twig" %}
    </div>
{% endblock %}
